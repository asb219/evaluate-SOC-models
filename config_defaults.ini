[path]
dump = dump
downloads = %(dump)s/Downloads
data = %(dump)s/Data
log = %(dump)s/Log
results = %(dump)s/Results

[log]
filename = log.log
logfile = enabled
console = enabled

[topsoil]
min_depth = 5
max_depth = 10
