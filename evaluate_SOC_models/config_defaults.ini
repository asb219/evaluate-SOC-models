[path]
repository = ..
log = %(repository)s/log
dump = %(repository)s/dump
downloads = %(dump)s/Downloads
data = %(dump)s/Data
results = %(dump)s/Results

[log]
console = enabled
logfile = enabled
filename = log.log
logfile_rotation = 500 KB
logfile_retention = 60 days

[topsoil]
min_depth = 5
max_depth = 10
